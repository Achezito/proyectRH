[build]
  publish = "RH_frontend/build"  # Si usas React/Vue/etc
  # O si es un backend Python:
  # command = "python backend/app.py"

[build.environment]
  PYTHON_VERSION = "3.11"
  NODE_VERSION = "18"  # Si también usas Node.js